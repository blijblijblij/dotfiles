# /Users/rogier/.config/tmuxinator/cfa.yml

name: rm-masterdata-engine 
root: ~/git/rm-masterdata-engine

on_project_start: ctags -R --languages=ruby --exclude=.git --exclude=log . $(bundle list --paths)

windows:
  - editor:
      layout: main-horizontal
      panes:
        - vim .
        - guard
