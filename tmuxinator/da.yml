# /Users/rogier/.config/tmuxinator/da.yml

name: rm-data-admin
root: ~/git/rm-data-admin

on_project_start: ctags -R --languages=ruby --exclude=.git --exclude=log . $(bundle list --paths)

windows:
  - editor: vim .
  - server: 
      layout: main-horizontal
      panes:
        - rails s -b 0.0.0.0 -p 3001
        - guard
